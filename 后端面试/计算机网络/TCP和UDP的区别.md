---
tags: [network]
source: "[[后端岗位面试题]]"
---

# 🌟TCP和UDP的区别

TCP 是一种**面向连接的可靠传输协议**，通过三次握手建立连接，并通过序号、确认应答、重传机制以及流量控制和拥塞控制来保证数据可靠、有序地到达。

UDP 是一种**无连接、不可靠的协议**，不保证数据可靠传输，也不保证顺序，但它的协议**开销小、延迟低**。

因此在实际应用中，如果对**数据可靠性要求高**，比如 HTTP、文件传输等场景通常使用 TCP；而对于**实时性要求较高**、允许少量丢包的场景，比如视频直播、在线游戏、语音通信等，通常会使用 UDP。
