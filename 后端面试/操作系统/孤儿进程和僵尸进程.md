---
tags: [os]
source: "[[后端岗位面试题]]"
---

# 孤儿进程和僵尸进程

孤儿进程：当**父进程先退出，子进程还没结束**时，该子进程就变成**孤儿进程**。孤儿进程会被 **init 进程（或 systemd）** 收养，并由它负责回收子进程的资源。



僵尸进程：当**子进程退出**后，其**退出状态仍保存在内核中**，供父进程通过 `wait()` 读取。解决方式：父进程调用 `wait()` / `waitpid()`；或让子进程被 init 收养。
