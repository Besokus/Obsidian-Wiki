---
tags: [os]
source: "[[后端岗位面试题]]"
---

# 如何创建子线程，fork返回的是什么

**如何创建子进程？**
调用 `fork()` 函数可以**创建子进程**。
它会复制当前进程（父进程）的地址空间，创建一个几乎相同的子进程。

**2️⃣ fork() 返回的是什么？**

- 在**父进程中**返回**子进程的 PID（大于 0）**；
- 在**子进程中**返回**0**；
- 如果创建失败，返回 **-1**。
