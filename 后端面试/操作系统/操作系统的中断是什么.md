---
tags: [os]
source: "[[后端岗位面试题]]"
---

# 操作系统的中断是什么

中断是操作系统用于处理**异步事件**的机制，能让 CPU 在执行程序时被“打断”，**优先处理更紧急的任务**，从而提高系统的实时性与效率。

**外部中断（硬件中断）：**
由外部设备产生，如键盘输入、定时器溢出、I/O 完成。
 👉 例如：你敲击键盘时，键盘控制器产生中断信号，通知 CPU 读取输入。

**内部中断（异常/陷阱）：**
由程序运行错误或特定指令触发，如除零错误、缺页中断、系统调用。
 👉 例如：访问不存在的内存地址会触发“缺页中断”。
