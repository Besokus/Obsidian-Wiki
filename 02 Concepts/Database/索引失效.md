---
type: concept
domain: database
tags: [mysql, index, query-optimization]
created: 2026-05-14
updated: 2026-05-14
---

# 索引失效

## 常见失效场景

| 场景 | 示例 |
|---|---|
| 使用 `!=` 或 `<>` | `WHERE age != 20` |
| 对字段做运算或函数 | `WHERE SUBSTRING(name, 1, 3) = 'abc'` |
| 模糊查询以通配符开头 | `WHERE name LIKE '%abc'` |
| 数据类型不一致 | `WHERE varchar_col = 123`（类型隐式转换） |
| `OR` 条件中有未索引列 | `WHERE a=1 OR unindexed_b=2` |
| 违背最左匹配原则 | 联合索引 `(a,b,c)`，但跳过了 `a` 直接查 `b` |

## 最左匹配原则详解

对于联合索引 `(a, b, c)`：

| 查询条件 | 是否命中 |
|---|---|
| `WHERE a=1` | ✓ |
| `WHERE a=1 AND b=2` | ✓ |
| `WHERE a=1 AND b=2 AND c=3` | ✓ |
| `WHERE b=2` | ✗（跳过了 a） |
| `WHERE a=1 AND c=3` | 部分（a 命中，c 不命中） |

## 注意

- `OR` 本身不会导致索引必然失效，只要各条件都用索引，MySQL 可能使用 Index Merge
- 最终是否使用索引取决于优化器的成本评估

---

## 相关笔记

- [[MySQL 索引]] — 索引类型与设计原则
- [[B+Tree 索引]] — 索引的底层数据结构
- [[MySQL 最左前缀法则]] — 最左匹配原则详解
